import * as React from "react";
import Layout from "../components/layout";
import LandingPage from "../components/landing";

const IndexPage = () => {
  return (
    <Layout pageTitle="Jedidiah Cares">
      <LandingPage />
    </Layout>
  );
};

export default IndexPage;
